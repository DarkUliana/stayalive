@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">EventLocation {{ $eventLocation->ID }}</div>
                    <div class="card-body">

                        <a href="{{ url('/event-locations') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/event-locations/' . $eventLocation->ID . '/edit') }}" title="Edit EventLocation"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('eventLocations' . '/' . $eventLocation->ID) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete EventLocation" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $eventLocation->ID }}</td>
                                    </tr>
                                    <tr><th> TriggerType </th><td> {{ $eventLocation->triggerType }} </td></tr><tr><th> ConditionName </th><td> {{ $eventLocation->conditionName }} </td></tr><tr><th> RespownCount </th><td> {{ $eventLocation->respownCount }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
