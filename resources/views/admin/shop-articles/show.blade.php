@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">shop article {{ $shopArticle->ID }}</div>
                    <div class="card-body">

                        <a href="{{ url('/shop-articles') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/shop-articles/' . $shopArticle->ID . '/edit') }}" title="Edit shop-article"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('shop-articles' . '/' . $shopArticle->ID) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete shop-article" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $shopArticle->ID }}</td>
                                    </tr>
                                    <tr><th> ShopID </th><td> {{ $shopArticle->shopID }} </td></tr><tr><th> ShopItemCategory </th><td> {{ $shopArticle->shopItemCategory }} </td></tr><tr><th> InGold </th><td> {{ $shopArticle->inGold }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
